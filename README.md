# Jensen Agent

A self-contained FastAPI service that wraps OpenAI’s `gpt-5` model.  
It runs locally on your NAS and is also reachable through the Synology reverse proxy.

---

##  Quick Start

```bash
docker compose up -d --build